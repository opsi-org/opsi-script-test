[Actions]
comment "Testing: "
message "GetValueFromInifile"
encoding=utf8

DefVar $TestResult$
DefVar $OS$
DefVar $HomeTestFiles$
DefVar $FilePath$
DefVar $value1$
DefVar $CompValue1$
DefVar $value2$
DefVar $CompValue2$

; This test needs opsi-script-test.opsiscript to be run first
; That will create the $HomeTestFiles$ file

Set $OS$ = GetOS

switch $OS$
	case "Windows_nt"
		comment "We are running on Windows"
		Set $HomeTestFiles$ = "%opsiTmpDir%\testFiles"
		Set $FilePath$ = "C:\Users\Jinene\Documents\gituib\opsi-script-test\CLIENT_DATA\test-files\ini"

	endcase
	case "Linux"
		comment "We are running on Linux"
		Set $HomeTestFiles$ = "/tmp/opsi-script-test/testFiles"
		Set $FilePath$ = "/home/jinene/gitwork/opsi-script-test/CLIENT_DATA/test-files/ini/"
	endcase
	case "macos"
		comment "We are running on macOS"
		Set $HomeTestFiles$ = "/tmp/opsi-script-test/testFiles"
	endcase
endswitch

;Set $HomeTestFiles$ = "test-files/"

Set $FilePath$ = $HomeTestFiles$+"dummy.ini"

if (FileExists($FilePath$))
	;GetValueFromInifile

	set $value1$ = "add1"
	Set $CompValue1$ = GetValueFromInifile($FilePath$, "secdummy", "dummy1", "default")
	
	set $value2$ = "üöäßÜÖÄ"
	Set $CompValue2$ = GetValueFromInifile($FilePath$, "secdüöä", "dümmy7", "default")
	
	if $value1$=$CompValue1$ AND $value2$=$CompValue2$
		comment "GetValueFromInifile passed"
	else
		set $TestResult$ = "not o.k."
		LogWarning "GetValueFromInifile failed"
	endif
else
	set $TestResult$ = "not o.k."
	logError "Fatal: could not create .ini file"
	isFatalError
endif

Set $FilePath$ = $HomeTestFiles$+"dummy_utf8.ini"

if (FileExists($FilePath$))
	;GetValueFromInifile

	set $value1$ = "add1"
	Set $CompValue1$ = GetValueFromInifile($FilePath$, "secdummy", "dummy1", "default")
	
	set $value2$ = "üöäßÜÖÄ"
	Set $CompValue2$ = GetValueFromInifile($FilePath$, "secdüöä", "dümmy7", "default")
	
	if $value1$=$CompValue1$ AND $value2$=$CompValue2$
		comment "GetValueFromInifile passed"
	else
		set $TestResult$ = "not o.k."
		LogWarning "GetValueFromInifile failed"
	endif
else
	set $TestResult$ = "not o.k."
	logError "Fatal: could not create .ini file"
	isFatalError
endif

Set $FilePath$ = $HomeTestFiles$+"dummy_utf16.ini"

if (FileExists($FilePath$))
	;GetValueFromInifile

	set $value1$ = "add1"
	Set $CompValue1$ = GetValueFromInifile($FilePath$, "secdummy", "dummy1", "default")
	
	set $value2$ = "üöäßÜÖÄ"
	Set $CompValue2$ = GetValueFromInifile($FilePath$, "secdüöä", "dümmy7", "default")
	
	if $value1$=$CompValue1$ AND $value2$=$CompValue2$
		comment "GetValueFromInifile passed"
	else
		set $TestResult$ = "not o.k."
		LogWarning "GetValueFromInifile failed"
	endif
else
	set $TestResult$ = "not o.k."
	logError "Fatal: could not create .ini file"
	isFatalError
endif
